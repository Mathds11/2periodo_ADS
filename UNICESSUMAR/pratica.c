#include <stdio.h>
int main(){
            int i, N = 3, res;
            for(i = 1; i <= 10; i++){
                        res = N * i;
                        printf("%d x %d = %d\n", i, N, res);
            }
}